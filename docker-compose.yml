# Use root/example as user/password credentials
version: '3.1'

services:

  db:
    image: percona
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: s3c4ndsys

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8088
